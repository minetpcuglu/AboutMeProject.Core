@model List<Last5NotificationDTO>
@{ string color = "";}
<div class="dropdown-menu dropdown-menu-right navbar-dropdown preview-list" aria-labelledby="notificationDropdown">

    <p class="mb-0 font-weight-normal float-left dropdown-header">Notifications</p>
    @foreach (var item in Model)
    {
        if (item.Status == "New Video")
        {
            color = "preview-icon bg-success";
        }
        if (item.Status == "Identity")
        {
            color = "preview-icon bg-warning";
        }
        if (item.Status == "AboutMe Project")
        {
            color = "preview-icon bg-danger";
        }
        if (item.Status == "Kastamonu University")
        {
            color = "preview-icon bg-info";
        }
        <a class="dropdown-item preview-item" href="/User/Default/Index/">
            <div class="preview-thumbnail">
                <div class="@color">
                    <i class="ti-info-alt mx-0"></i>
                </div>
            </div>
            <div class="preview-item-content">
                <h6 class="preview-subject font-weight-normal">  @item.Title</h6>
                <p class="font-weight-light small-text mb-0 text-muted">
                    @item.Status
                </p>
            </div>
        </a>

    }

</div>